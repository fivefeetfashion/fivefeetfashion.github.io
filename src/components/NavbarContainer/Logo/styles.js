import helpers from '../../../utils/helpers.js';

const styles = {
    img: {
        width   : '100%',
        height  : '100%',
        ...(helpers.primaryBackground(0.8)) 
    }
};

export default styles;
